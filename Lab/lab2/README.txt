Class lab2

    Method isAnagram takes two strings s and t
        Convert both s and t into character arrays sArray and tArray respectively

        Sort both sArray and tArray

        Compare sArray and tArray for equality
            If they are equal
                Return true
            Else
                Return false

    End Method

    Main Method
        Create an instance of lab2 named checker

        Print the result of calling checker.isAnagram with "listen" and "silent"
            Expected to print true

        Print the result of calling checker.isAnagram with "hello" and "world"
            Expected to print false

